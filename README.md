A simple example of an e-commerce website, built with ReactJS.
It uses JWT for authentication and json-server for backend.
Run './node_modules/.bin/json-server-auth ./backend/db.json --port 3001' to start backend.
Then, use 'yarn start' or 'npm start' to run the project.
